from setuptools import find_packages, setup

setup(
      name = 'ETE LANGCHAIN PDF QA FAISS'  
      version='0.0'
      author='Parthebhan Pari'  
      author_email='@'
      packages=find_packages(),
      install_requires=[]

)